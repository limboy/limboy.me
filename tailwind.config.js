module.exports = {
  content: ['./templates/**/*.html'],
  theme: {
    extend: {
      colors: {
        'nav-color': 'var(--nav-color)',
      },
    },
  },
  // plugins: [require('@tailwindcss/typography')],
};
